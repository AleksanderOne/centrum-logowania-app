name: 🚀 Generowanie Wersji

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  generowanie-wersji:
    name: 📦 Generowanie Wersji Aplikacji
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Pobieranie Kodu
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: ⚙️ Konfiguracja Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: 📚 Instalacja Zależności
        run: npm ci

      - name: 🏷️ Generowanie Wersji i Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HUSKY: '0'
        run: |
          echo "🚀 Rozpoczynam generowanie nowej wersji..."
          npx semantic-release
          echo "✅ Generowanie wersji zakończone!"
